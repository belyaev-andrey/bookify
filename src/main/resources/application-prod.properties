#
# Test
#

#
# Test
#

spring.application.name=bookify-prod
spring.datasource.url=jdbc:postgresql://127.0.0.1:50253/bookify
spring.datasource.username=bookify
spring.datasource.password=secret
spring.datasource.driver-class-name=org.postgresql.Driver

bookify.payments.provider=production
bookify.payments.production.base-url=${PAYMENT_GATEWAY_BASE_URL:https://api.payment-gateway.example.com}
bookify.payments.production.api-key=${PAYMENT_GATEWAY_API_KEY:}
spring.docker.compose.enabled=false
